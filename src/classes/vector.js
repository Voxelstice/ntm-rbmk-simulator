// Vector-helper vector3 only
class Vec3 {
    constructor(x, y, z) {
        this.xCoord = x
        this.yCoord = y
        this.zCoord = z
    }

    rotateAroundY(amount) {
        // Help needed
        this.yCoord += amount
    }
}
